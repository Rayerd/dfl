{
    "version": "0.2.0",
    "configurations": [
        {
            "name": "C++ Launch (Windows) scrollbar",
            "type": "cppvsdbg",
            "request": "launch",
            "cwd": "${workspaceRoot}",
            "program": "./bin/scrollbar.exe",
            "console": "internalConsole"
        }
    ]
}