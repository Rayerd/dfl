{
    "version": "0.2.0",
    "configurations": [
        {
            "name": "C++ Launch (Windows) toastnotifier",
            "type": "cppvsdbg",
            "request": "launch",
            "cwd": "${workspaceRoot}",
            "program": "./bin/toastnotifier_sample.exe",
            "console": "integratedTerminal"
        }
    ]
}