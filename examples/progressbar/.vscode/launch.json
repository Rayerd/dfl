{
    "version": "0.2.0",
    "configurations": [
        {
            "name": "C++ Launch (Windows) progressbar",
            "type": "cppvsdbg",
            "request": "launch",
            "cwd": "${workspaceRoot}",
            "program": "./bin/progressbar_sample.exe",
            "console": "internalConsole"
        }
    ]
}