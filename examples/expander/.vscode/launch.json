{
    "version": "0.2.0",
    "configurations": [
        {
            "name": "C++ Launch (Windows) expander",
            "type": "cppvsdbg",
            "request": "launch",
            "cwd": "${workspaceRoot}",
            "program": "./bin/expander.exe",
            "console": "internalConsole"
        }
    ]
}