{
    "version": "0.2.0",
    "configurations": [
        {
            "name": "C++ Launch (Windows) registry",
            "type": "cppvsdbg",
            "request": "launch",
            "cwd": "${workspaceRoot}",
            "program": "./bin/registry_sample.exe",
            "console": "internalConsole"
        }
    ]
}